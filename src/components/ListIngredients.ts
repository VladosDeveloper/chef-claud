import styled from "styled-components";

export const ListIngredients = styled.ul`
    font-weight: 400;
    font-size: 18px;
    line-height: 1.3;

    li {
        margin: 5px 20px;
        text-transform: capitalize;
    }

    p {
        padding: 15px 0;
    }
`